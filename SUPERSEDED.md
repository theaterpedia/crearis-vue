# ⚠️ `alpha/magnifica-site` is SUPERSEDED — do not build on it

**This branch is outdated. Its content is no longer valid.**

The current magnifica **content and implementation** live on **`alpha/magnifica-final`** (the
deploy lane). `alpha/magnifica-site` was the **cand-1c** candidate line; it diverged at `6b45122`
(2026-06-02) and has since been overtaken.

`alpha/magnifica-final` holds:
- the **current content** (e.g. the 2026-06-08 English-spine `/context`, the landing honest-flag
  close, the `/discourse` + `/ethnography` rewrites) — newer than anything on this branch;
- the **BackSlide-stack**, `MagnificaChatbox`, footer + Impressum, and the canonical page-shell
  (`MagnificaPageLayout` + `magnifica-page.css`).

**Do not take content or components from here as current.** Start from `alpha/magnifica-final`.

— recorded 2026-06-10 (HM)
